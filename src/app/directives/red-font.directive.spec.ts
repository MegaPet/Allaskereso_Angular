import { RedFontDirective } from './red-font.directive';

describe('RedFontDirective', () => {
  it('should create an instance', () => {
    const directive = new RedFontDirective();
    expect(directive).toBeTruthy();
  });
});
